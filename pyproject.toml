[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mutadock"
version = "2.1.0"
authors = [
  {name = "Naisarg Patel", email = "naisarg.patel14@hotmail.com"}
]
description = "MUTADOCK is a comprehensive library designed for mutation studies and multiple receptor-ligand docking. Refer to README for more information."
readme = "README.md"
license = {text = "GPL-3.0"}
keywords = ["Biology", "Mutation", "Docking", "Bioinformatics"]
dependencies = [
  'biopython',
  'meeko',
  'numpy',
  'openmm',
  'pdbfixer',
  'pyrosetta-installer',
  'tqdm',
  'pyarrow',
  'pandas',
  'matplotlib',
  'python-pptx',
  'jinja2',
]


classifiers = [
  "Development Status :: 4 - Beta",

  "Intended Audience :: Education",
  "Topic :: Scientific/Engineering :: Bio-Informatics",

  "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",


  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12"
]

[tool.hatch.build.targets.wheel]
packages = ["src/mutadock"]
# Non-Python data under the package (vendored NGL + Jinja templates in
# src/mutadock/report/{assets,templates}) is included in the wheel by default
# because it lives inside the packaged directory.

[project.scripts]
md_mutate = "mutadock.mutation.np_mutation:np_mutation"
md_dock = "mutadock.docking.np_docking:np_docking"
md_quick = "mutadock.quick:md_quick"
md_vina_dock = "mutadock.docking.vina_dock:main"
md_csv_generator = "mutadock.mutation.csv_generator:main"
md_csv_sort = "mutadock.mutation.csv_sort:main"
md_ddg_single = "mutadock.mutation.ddg_calc:main"
md_ddg_double = "mutadock.mutation.ddg_calc_double:main"
md_ddg_triple = "mutadock.mutation.ddg_calc_triple:main"
md_generate_pdb = "mutadock.mutation.generate_mutant_pdb:main"
md_report = "mutadock.report.report:md_report"


[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "pytest-cov>=4.0",
  "pytest-mock>=3.0",
  "coverage[toml]>=7.0",
  "pre-commit>=3.0",
  "black>=24.0",
  "ruff>=0.4",
  "mypy>=1.10",
  "isort>=5.13",
]
docs = [
  "sphinx>=7.0",
  "alabaster>=0.7",
]

[project.urls]
Repository = "https://github.com/naisarg14/mutadock"
Issues = "https://github.com/naisarg14/mutadock/issues"
Documentation = "https://mutadock.readthedocs.io/en/latest/"

# ---------------------------------------------------------------------------
# pytest
# ---------------------------------------------------------------------------

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
pythonpath = ["src"]
addopts = [
  "--strict-markers",
  "-v",
]
markers = [
  "mutation: tests for the mutation module",
  "docking: tests for the docking module",
  "unit: fast unit tests with mocked dependencies",
  "slow: computationally expensive tests requiring real PyRosetta/Vina",
]

# ---------------------------------------------------------------------------
# coverage
# ---------------------------------------------------------------------------

[tool.coverage.run]
source = ["src"]
branch = true
omit = [
  "tests/*",
  "src/*/__init__.py",
  "*/conftest.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
precision = 2
exclude_lines = [
  "pragma: no cover",
  "if __name__ == .__main__.",
  "raise NotImplementedError",
  '\.\.\.',
]

[tool.coverage.html]
directory = "htmlcov"

# ---------------------------------------------------------------------------
# black
# ---------------------------------------------------------------------------

[tool.black]
line-length = 88
target-version = ["py311", "py312"]

# ---------------------------------------------------------------------------
# isort
# ---------------------------------------------------------------------------

[tool.isort]
profile = "black"
src_paths = ["src", "tests"]
known_first_party = ["mutadock"]

# ---------------------------------------------------------------------------
# ruff
# ---------------------------------------------------------------------------

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "B", "UP"]
ignore = [
  "E501",   # line length — enforced by black
  "UP007",  # use X | Y for union types — Optional kept for clarity (covers Optional too in this ruff version)
]

[tool.ruff.lint.per-file-ignores]
# pyrosetta is imported via `from pyrosetta import *` inside a try block;
# F403 = star import, F405 = name may be from star import
"src/mutadock/mutation/ddg_calc.py"        = ["F403", "F405"]
"src/mutadock/mutation/ddg_calc_double.py" = ["F403", "F405"]
"src/mutadock/mutation/ddg_calc_triple.py" = ["F403", "F405"]
"src/mutadock/mutation/generate_mutants.py" = ["F403", "F405"]
"src/mutadock/mutation/generate_mutant_pdb.py" = ["F403", "F405"]
"src/mutadock/mutation/predict_ddG.py"     = ["F403", "F405"]
# test files use wildcard fixture injection and broad mocks
"tests/**/test_*.py"              = ["F811"]

# ---------------------------------------------------------------------------
# mypy
# ---------------------------------------------------------------------------

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
strict_optional = true
warn_unused_ignores = true
warn_unreachable = true

[[tool.mypy.overrides]]
module = [
    "mutadock.mutation.ddg_calc",
    "mutadock.mutation.ddg_calc_double",
    "mutadock.mutation.ddg_calc_triple",
    "mutadock.mutation.generate_mutants",
    "mutadock.mutation.predict_ddG",
]
disable_error_code = ["name-defined"]
